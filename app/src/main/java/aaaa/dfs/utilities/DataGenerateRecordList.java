package aaaa.dfs.utilities;

import static java.time.MonthDay.now;

import android.annotation.SuppressLint;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

import aaaa.dfs.model.Record;
import aaaa.dfs.model.RecordList;

public class DataGenerateRecordList {



   // @SuppressLint("NewApi")
    public static RecordList generateRecordList()
    {

//        DateTimeFormatter dtf = DateTimeFormatter.ofPattern("yyyy/MM/dd HH:mm:ss");
//        LocalDateTime now = LocalDateTime.now();
//        RecordList recordList = new RecordList();
//        recordList.addRecord(new Record(100, dtf.format(now)));
//        recordList.addRecord(new Record(99, dtf.format(now)));
//        recordList.addRecord(new Record(101, dtf.format(now)));
//        recordList.addRecord(new Record(200, dtf.format(now)));
//        return recordList;
        return null;
    }




}
